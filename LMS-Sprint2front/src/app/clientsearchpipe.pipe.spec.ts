import { ClientsearchpipePipe } from './clientsearchpipe.pipe';

describe('ClientsearchpipePipe', () => {
  it('create an instance', () => {
    const pipe = new ClientsearchpipePipe();
    expect(pipe).toBeTruthy();
  });
});
